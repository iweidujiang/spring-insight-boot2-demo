spring:
  application:
    name: boot2-demo-consumer
  insight:
    server-url: http://localhost:9966
    diagnostic-logs: false

server:
  port: 18090

demo:
  # url 直连无需改 hosts；拓扑 remoteService 优先取 @FeignClient 的 name
  provider-url: http://127.0.0.1:18091
